# diwizi-reels

Media host for Diwizi social (Instagram + Facebook) Reels, generated by the
automated posting routine. Files under `reels/` are served publicly via
`raw.githubusercontent.com` so PostProxy can fetch them by URL.

Not meant to be browsed — just a public blob store for the automation.
